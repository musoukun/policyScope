{
  "functions": {
    "frontend/app/api/**/route.ts": {
      "maxDuration": 30
    }
  },
  "buildCommand": "cd frontend && npm run build && cd ../backend && npx mastra build",
  "outputDirectory": "frontend/.next",
  "installCommand": "cd frontend && npm install && cd ../backend && npm install",
  "framework": "nextjs",
  "env": {
    "MASTRA_API_URL": "https://policyscope-mastra.vercel.app"
  }
}